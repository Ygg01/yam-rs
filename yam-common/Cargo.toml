[package]
name = "yam-common"
version = "0.1.0"
edition = "2024"

[dependencies]
ordered-float = { version = "5.3.0", optional = true }

[features]
default = ["hashed_node"]
hashed_node = ["ordered-float"]
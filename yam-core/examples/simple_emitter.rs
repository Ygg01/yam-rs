extern crate yam_core;

fn main() {
    let mut string = String::new();
    // let mut emitter: yam_core::prelude::YamlEmitter<'_, FP, INT> = YamlEmitter::new(&mut string);

    // let yaml = Yaml(YamlD;
    // emitter.dump();
}

# Coding

## Structure

- yam-core - `no_std` + `alloc` lib that contains the YAML processing logic
- yam-std - `std` lib that adds io support to yam-core.
- yam-serde - `no_std` + `alloc` lib that provides `serde` integration